# Inspect Pull Request

Inspect the pull request:

- PR: {props.pr}
- repository: {props.repo}

Use `gh` to gather:

- overall PR state
- check status
- requested changes or actionable review comments
- whether the next action is `clean`, `needs-fix`, `needs-human`, or `waiting`

REQUIRED OUTPUT:
{props.schema}
