# Review

Reviewer: {props.reviewer}

Review the current repository changes with a strict production bar.

Prioritize:

- safety and correctness
- regression risk
- scope control
- maximum leanness
- rejection of unnecessary abstractions, boilerplate, or defensive code that is not justified

The best line of code is the one not written.

Approve only if the implementation is both correct and already close to the leanest reasonable shape.

REQUEST:
{props.prompt}

REQUIRED OUTPUT:
{props.schema}
